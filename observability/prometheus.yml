global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts-runs.yml

scrape_configs:
  - job_name: "financial-project"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["host.docker.internal:8090"]
